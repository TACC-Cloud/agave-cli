sudo: required

language: python

addons:
    apt:
        packages:
            - docker-ce

services:
    - docker

before_install:
    - cp tests.Dockerfile tests/
    - cd tests/
    - pwd
    - ls
    - docker build --force-rm -f tests.Dockerfile -t "agavecli-dev" .
    - cd ../
    - pwd
    - ls
    - git log --oneline -n 1

script:
    - ./travis_tests.sh
